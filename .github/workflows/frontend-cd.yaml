name: cd-frontend-workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{env.NODE_VERSION}}

      - name: Install dependencies
        run: npm install
        working-directory: /home/runner/work/Project-4-AWS-Final/Project-4-AWS-Final/starter/frontend/

      - name: Run linter
        run: npm run lint
        working-directory: /home/runner/work/Project-4-AWS-Final/Project-4-AWS-Final/starter/frontend/

      - name: Run tests
        run: npm run test
        working-directory: /home/runner/work/Project-4-AWS-Final/Project-4-AWS-Final/starter/frontend/

  deploy:
    needs: [lint-and-test]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: 741704027807.dkr.ecr.us-east-1.amazonaws.com/frontend:latest

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Deploy to Kubernetes
        run: |
          kustomize build